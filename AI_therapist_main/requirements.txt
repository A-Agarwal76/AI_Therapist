# Core dependencies for Medical Data Analysis RAG project (Free stack)
# Updated for Python 3.13 compatibility and to keep heavy deps optional.

# LangChain + Chroma (pin to known-good minor versions)
langchain==0.1.16
langchain-community==0.0.35
langchain-chroma==0.1.4
chromadb==0.4.24
# Pin PostHog to avoid API breakage with Chroma telemetry
posthog<3
python-dotenv==1.0.1

# Hybrid retrieval (BM25 + Dense)
rank-bm25==0.2.2

# Embeddings
sentence-transformers==3.0.1
huggingface-hub==0.24.5

# Torch-free embedding backend (recommended on macOS/Python 3.13)
fastembed==0.5.1

# Transformers stack (generation). Torch is OPTIONAL on Python 3.13/macOS.
# If you can install torch, these work together; otherwise the app runs in retrieval-only mode.
accelerate==0.33.0
safetensors==0.4.4
tokenizers==0.21.2
transformers==4.47.0

# NOTE: PyTorch wheels for Python 3.13/macOS may not be available.
# If you want local generation and have a supported environment, install torch separately, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

# Utilities / HTTP
requests==2.32.3

# Data processing (NumPy 2.x for Python 3.13; Pandas 2.2.3 adds Py3.13 wheels)
pandas==2.2.3
numpy==2.1.3
pyarrow==18.1.0

# UI
streamlit==1.39.0
streamlit-mic-recorder==0.0.8
av==16.0.1

# ASR for voice input
faster-whisper==1.2.1

# Optional: language detection improvement
langdetect==1.0.9

# Optional: translation fallback (no API key)
deep-translator==1.11.4
